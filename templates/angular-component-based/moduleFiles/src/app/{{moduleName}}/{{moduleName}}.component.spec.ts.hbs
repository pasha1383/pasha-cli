import { ComponentFixture, TestBed } from '@angular/core/testing';
import { {{pascalCase moduleName}}Component } from './{{moduleName}}.component';

describe('{{pascalCase moduleName}}Component', () => {
  let component: {{pascalCase moduleName}}Component;
  let fixture: ComponentFixture<{{pascalCase moduleName}}Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [{{pascalCase moduleName}}Component],
    }).compileComponents();

    fixture = TestBed.createComponent({{pascalCase moduleName}}Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should render title', () => {
    const h2 = fixture.nativeElement.querySelector('h2');
    expect(h2.textContent).toContain('{{pascalCase moduleName}}');
  });

  it('should increment count', () => {
    const btn = fixture.nativeElement.querySelectorAll('button')[2];
    btn.click();
    fixture.detectChanges();
    const count = fixture.nativeElement.querySelector('.count');
    expect(count.textContent.trim()).toBe('1');
  });

  it('should decrement count', () => {
    component.count.set(5);
    const btn = fixture.nativeElement.querySelectorAll('button')[0];
    btn.click();
    fixture.detectChanges();
    const count = fixture.nativeElement.querySelector('.count');
    expect(count.textContent.trim()).toBe('4');
  });

  it('should reset count', () => {
    component.count.set(42);
    const btn = fixture.nativeElement.querySelectorAll('button')[1];
    btn.click();
    fixture.detectChanges();
    const count = fixture.nativeElement.querySelector('.count');
    expect(count.textContent.trim()).toBe('0');
  });
});
