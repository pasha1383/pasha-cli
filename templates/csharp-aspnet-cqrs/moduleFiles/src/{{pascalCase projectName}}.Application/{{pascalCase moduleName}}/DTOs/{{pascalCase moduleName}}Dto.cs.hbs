namespace {{pascalCase projectName}}.Application.{{pascalCase moduleName}}.DTOs;

{{#if useFluentValidation}}
using FluentValidation;
{{/if}}

public record Create{{pascalCase moduleName}}Request(string Name);

public record {{pascalCase moduleName}}Response(
    {{#if ormEfcore}}int{{else}}string{{/if}} Id,
    string Name,
    DateTime CreatedAt,
    DateTime UpdatedAt
);

{{#if useFluentValidation}}
public class Create{{pascalCase moduleName}}Validator : AbstractValidator<Create{{pascalCase moduleName}}Request>
{
    public Create{{pascalCase moduleName}}Validator()
    {
        RuleFor(x => x.Name)
            .NotEmpty()
            .MaximumLength(255);
    }
}
{{/if}}
