package application

import (
{{#if ormNone}}
	"github.com/google/uuid"
{{/if}}

	"{{github}}/{{projectName}}/internal/domain"
)

type {{pascalCase moduleName}}UseCase struct {
	repo domain.{{pascalCase moduleName}}Repository
}

func New{{pascalCase moduleName}}UseCase(repo domain.{{pascalCase moduleName}}Repository) *{{pascalCase moduleName}}UseCase {
	return &{{pascalCase moduleName}}UseCase{repo: repo}
}

func (uc *{{pascalCase moduleName}}UseCase) Create(name string) (*domain.{{pascalCase moduleName}}, error) {
	model := &domain.{{pascalCase moduleName}}{
{{#if ormGorm}}
		Name: name,
{{else}}
		ID:   uuid.New().String(),
		Name: name,
{{/if}}
	}
	if err := uc.repo.Save(model); err != nil {
		return nil, err
	}
	return model, nil
}

func (uc *{{pascalCase moduleName}}UseCase) List() ([]domain.{{pascalCase moduleName}}, error) {
	return uc.repo.FindAll()
}

func (uc *{{pascalCase moduleName}}UseCase) GetByID(id {{#if ormGorm}}uint{{else}}string{{/if}}) (*domain.{{pascalCase moduleName}}, error) {
	return uc.repo.FindByID(id)
}

func (uc *{{pascalCase moduleName}}UseCase) Delete(id {{#if ormGorm}}uint{{else}}string{{/if}}) error {
	return uc.repo.Delete(id)
}
