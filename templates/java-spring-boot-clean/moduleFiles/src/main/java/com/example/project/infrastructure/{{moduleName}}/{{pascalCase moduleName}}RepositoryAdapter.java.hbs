package com.example.project.infrastructure.{{moduleName}};

import com.example.project.domain.{{moduleName}}.{{pascalCase moduleName}};
import com.example.project.domain.{{moduleName}}.{{pascalCase moduleName}}Repository;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;

@Component
public class {{pascalCase moduleName}}RepositoryAdapter implements {{pascalCase moduleName}}Repository {

    private final {{pascalCase moduleName}}JpaRepository jpaRepository;

    public {{pascalCase moduleName}}RepositoryAdapter({{pascalCase moduleName}}JpaRepository jpaRepository) {
        this.jpaRepository = jpaRepository;
    }

    @Override
    public {{pascalCase moduleName}} save({{pascalCase moduleName}} entity) {
        var jpaEntity = {{pascalCase moduleName}}JpaEntity.from(entity);
        var saved = jpaRepository.save(jpaEntity);
        return saved.toDomain();
    }

    @Override
    public Optional<{{pascalCase moduleName}}> findById(Long id) {
        return jpaRepository.findById(id).map({{pascalCase moduleName}}JpaEntity::toDomain);
    }

    @Override
    public List<{{pascalCase moduleName}}> findAll() {
        return jpaRepository.findAll().stream()
                .map({{pascalCase moduleName}}JpaEntity::toDomain)
                .collect(Collectors.toList());
    }

    @Override
    public void deleteById(Long id) {
        jpaRepository.deleteById(id);
    }
}
