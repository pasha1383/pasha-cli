package com.example.project.adapter.out.persistence;

{{#if ormJpa}}
import jakarta.persistence.*;
{{/if}}

{{#if ormJpa}}
@Entity
@Table(name = "{{snakeCase moduleName}}")
{{/if}}
public class {{pascalCase moduleName}}Entity {
    {{#if ormJpa}}
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    {{/if}}
    private Long id;

    {{#if ormJpa}}
    @Column(nullable = false)
    {{/if}}
    private String name;

    public {{pascalCase moduleName}}Entity() {
    }

    public {{pascalCase moduleName}}Entity(Long id, String name) {
        this.id = id;
        this.name = name;
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public static {{pascalCase moduleName}}Entity from(com.example.project.domain.model.{{pascalCase moduleName}} domain) {
        return new {{pascalCase moduleName}}Entity(domain.getId(), domain.getName());
    }

    public com.example.project.domain.model.{{pascalCase moduleName}} toDomain() {
        return new com.example.project.domain.model.{{pascalCase moduleName}}(id, name);
    }
}
