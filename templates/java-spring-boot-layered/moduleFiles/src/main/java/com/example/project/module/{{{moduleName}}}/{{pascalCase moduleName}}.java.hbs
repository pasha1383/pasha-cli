package com.example.project.module.{{moduleName}};
{{#if ormJpa}}
import jakarta.persistence.*;
{{/if}}

{{#if ormJpa}}
@Entity
@Table(name = "{{snakeCase moduleName}}")
{{/if}}
public class {{pascalCase moduleName}} {
    {{#if ormJpa}}
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    {{/if}}
    private Long id;

    {{#if ormJpa}}
    @Column(nullable = false)
    {{/if}}
    private String name;

    public {{pascalCase moduleName}}() {
    }

    public {{pascalCase moduleName}}(Long id, String name) {
        this.id = id;
        this.name = name;
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }
}
