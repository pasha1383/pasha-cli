import { {{pascalCase moduleName}} } from '../../../core/entities/{{moduleName}}.entity';

export interface {{pascalCase moduleName}}View {
  id: string;
  name: string;
}

/**
 * Output boundary. Use-cases return domain entities; the presenter decides
 * how they are shaped for the outside world. Changing the API response
 * format should touch this file and nothing in core/.
 */
export class {{pascalCase moduleName}}Presenter {
  toView(entity: {{pascalCase moduleName}}): {{pascalCase moduleName}}View {
    return { id: entity.id, name: entity.name };
  }

  toViewList(entities: {{pascalCase moduleName}}[]): {{pascalCase moduleName}}View[] {
    return entities.map((e) => this.toView(e));
  }
}
