import { I{{pascalCase moduleName}}Repository } from '../domain/{{moduleName}}.repository.interface';
import { NotFoundError } from '../../../errors/app-error';
import { Get{{pascalCase moduleName}}Query } from './get-{{moduleName}}.query';
import { {{pascalCase moduleName}}ResponseDto } from '../dto/{{moduleName}}.response';
import { IQueryHandler } from '../infrastructure/bus';

export class Get{{pascalCase moduleName}}Handler implements IQueryHandler<Get{{pascalCase moduleName}}Query, {{pascalCase moduleName}}ResponseDto> {
  constructor(private readonly repo: I{{pascalCase moduleName}}Repository) {}

  async execute(query: Get{{pascalCase moduleName}}Query): Promise<{{pascalCase moduleName}}ResponseDto> {
    const entity = await this.repo.findById(query.id);
    if (!entity) {
      throw new NotFoundError(`{{pascalCase moduleName}} with id "${query.id}" not found`);
    }
    return {
      id: entity.id,
      name: entity.name,
      createdAt: entity.createdAt.toISOString(),
      updatedAt: entity.updatedAt.toISOString(),
    };
  }
}
