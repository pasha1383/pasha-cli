{{#if useExpressValidator}}
import { body } from 'express-validator';

export const create{{pascalCase moduleName}}Validation = [
  body('name').isString().isLength({ min: 1, max: 255 }).withMessage('name is required (1–255 chars)'),
];
{{/if}}
{{#if useZod}}
import { z } from 'zod';

export const create{{pascalCase moduleName}}Schema = z.object({
  name: z.string().min(1).max(255),
});

export type Create{{pascalCase moduleName}}Dto = z.infer<typeof create{{pascalCase moduleName}}Schema>;
{{/if}}
