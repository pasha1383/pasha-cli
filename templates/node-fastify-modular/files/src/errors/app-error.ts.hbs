/**
 * Base class for errors meant to reach the client as a specific HTTP status.
 * Services throw these; the error handler plugin translates them to a
 * response. Anything else thrown is treated as a 500 and never leaks its
 * message to the client.
 */
export class AppError extends Error {
  constructor(
    message: string,
    public readonly statusCode: number = 400,
  ) {
    super(message);
    this.name = 'AppError';
  }
}

export class NotFoundError extends AppError {
  constructor(message: string) {
    super(message, 404);
    this.name = 'NotFoundError';
  }
}

export class ValidationError extends AppError {
  constructor(message: string) {
    super(message, 422);
    this.name = 'ValidationError';
  }
}
