# {{projectName}}

{{description}}

Built with pasha CLI — **Onion Architecture** on Fastify.

## Stack

- **Framework:** Fastify (high-performance, plugin-based)
- **Architecture:** Onion (Domain → Application → Infrastructure → Presentation)
- **Data layer:** {{#if ormNone}}in-memory repositories{{else}}{{orm}}{{/if}}
{{#if hasDatabase}}
- **Database:** {{database}}
{{/if}}
- **Validation:** {{#if hasValidation}}{{validation}}{{else}}none{{/if}}
{{#if useRedis}}
- **Cache:** Redis
{{/if}}
{{#if useSwagger}}
- **API docs:** Swagger UI at `/api/docs`
{{/if}}

## Architecture

```
src/
├── domain/                  ← Innermost: entities, repository interfaces
├── application/             ← Use cases, DTOs, service orchestration
├── infrastructure/          ← Persistence adapters (Prisma/TypeORM/Mongoose/In-Memory)
└── presentation/            ← Outermost: Fastify route plugins
```

Each module exports a Fastify plugin (`export default async function`).
The plugin handles its own wiring and registers routes on the Fastify instance.
In `app.ts`, each plugin is registered with its prefix.

## Run

```bash
cp .env.example .env
{{#if useDocker}}
npm run infra:up
{{/if}}
npm install
{{#if ormPrisma}}
npm run prisma:migrate
{{/if}}
npm run start:dev
```

## Modules

{{#each modules}}
- `{{this}}` → `http://localhost:3000/{{this}}`
{{/each}}

---
Made with {{author}}
