import { randomUUID } from 'crypto';
import { {{pascalCase moduleName}} } from '../domain/{{moduleName}}.entity';
import { I{{pascalCase moduleName}}Repository } from '../domain/{{moduleName}}.repository.interface';
import { {{pascalCase moduleName}}CreatedEvent } from '../domain/{{moduleName}}.events';
import { Create{{pascalCase moduleName}}Command } from './create-{{moduleName}}.command';
import { ICommandHandler } from '../infrastructure/bus';

export class Create{{pascalCase moduleName}}Handler implements ICommandHandler<Create{{pascalCase moduleName}}Command, {{pascalCase moduleName}}CreatedEvent> {
  constructor(private readonly repo: I{{pascalCase moduleName}}Repository) {}

  async execute(command: Create{{pascalCase moduleName}}Command): Promise<{{pascalCase moduleName}}CreatedEvent> {
    const entity = new {{pascalCase moduleName}}(randomUUID(), command.name);
    await this.repo.save(entity);
    return new {{pascalCase moduleName}}CreatedEvent(entity);
  }
}
