import { Module } from '@nestjs/common';
import { {{pascalCase moduleName}}Module } from './infrastructure/{{moduleName}}/{{moduleName}}.module';

@Module({
  imports: [{{pascalCase moduleName}}Module],
})
export class AppModule {}
