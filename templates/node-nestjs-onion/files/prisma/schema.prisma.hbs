generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

{{#each modules}}
model {{pascalCase this}} {
  id   String @id
  name String

  @@map("{{snakeCase this}}")
}
{{/each}}
