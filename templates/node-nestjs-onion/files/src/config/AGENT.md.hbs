# src/config/

Application-wide configuration loaded via `@nestjs/config`.

- `app.config.ts` — loads `.env` globally
- `validation.pipe.ts` — class-validator global pipe
- `zod.pipe.ts` — Zod-based validation pipe
- `swagger.ts` — Swagger/OpenAPI setup
