<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;
use Src\Infrastructure\Http\Controllers\\{{pascalCase moduleName}}Controller;

Route::prefix('api')->group(function () {
    {{#if useHealthCheck}}
    Route::get('/health', [\App\Http\Controllers\HealthController::class, 'index']);
    {{/if}}

    {{#each modules}}
    Route::apiResource('{{this}}', \Src\Infrastructure\Http\Controllers\\{{pascalCase this}}Controller::class);
    {{/each}}
});
