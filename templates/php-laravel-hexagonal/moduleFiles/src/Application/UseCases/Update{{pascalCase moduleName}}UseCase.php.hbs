<?php

declare(strict_types=1);

namespace Src\Application\UseCases;

use Src\Application\Ports\{{pascalCase moduleName}}RepositoryPort;
use Src\Domain\{{pascalCase moduleName}};
use Symfony\Component\HttpKernel\Exception\NotFoundHttpException;

class Update{{pascalCase moduleName}}UseCase
{
    public function __construct(
        private readonly {{pascalCase moduleName}}RepositoryPort $repository,
    ) {
    }

    public function execute(int $id, array $data): {{pascalCase moduleName}}
    {
        $existing = $this->repository->findById($id);

        if ($existing === null) {
            throw new NotFoundHttpException('{{pascalCase moduleName}} not found.');
        }

        $updated = new {{pascalCase moduleName}}(
            id: $existing->id,
            name: $data['name'] ?? $existing->name,
            description: $data['description'] ?? $existing->description,
            createdAt: $existing->createdAt,
            updatedAt: new \DateTimeImmutable(),
        );

        return $this->repository->save($updated);
    }
}
