<?php

declare(strict_types=1);

namespace App\Repositories;

use App\Models\\{{pascalCase moduleName}};
use Illuminate\Database\Eloquent\Collection;

class {{pascalCase moduleName}}Repository
{
    public function all(): Collection
    {
        return {{pascalCase moduleName}}::all();
    }

    public function findById(int $id): ?{{pascalCase moduleName}}
    {
        return {{pascalCase moduleName}}::find($id);
    }

    public function create(array $data): {{pascalCase moduleName}}
    {
        return {{pascalCase moduleName}}::create($data);
    }

    public function update({{pascalCase moduleName}} $model, array $data): {{pascalCase moduleName}}
    {
        $model->update($data);

        return $model->refresh();
    }

    public function delete({{pascalCase moduleName}} $model): void
    {
        $model->delete();
    }
}
