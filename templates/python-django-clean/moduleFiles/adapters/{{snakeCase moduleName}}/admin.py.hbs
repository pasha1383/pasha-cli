from django.contrib import admin

from .repository import {{pascalCase moduleName}}Model


@admin.register({{pascalCase moduleName}}Model)
class {{pascalCase moduleName}}Admin(admin.ModelAdmin):
    list_display = ['id', 'name']
    search_fields = ['name']
