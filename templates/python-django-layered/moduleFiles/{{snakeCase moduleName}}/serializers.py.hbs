from rest_framework import serializers

from .models import {{pascalCase moduleName}}


class {{pascalCase moduleName}}Serializer(serializers.ModelSerializer):
    class Meta:
        model = {{pascalCase moduleName}}
        fields = ['id', 'name']
        read_only_fields = ['id']
