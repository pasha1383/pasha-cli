from django.contrib import admin

from .models import {{pascalCase moduleName}}


@admin.register({{pascalCase moduleName}})
class {{pascalCase moduleName}}Admin(admin.ModelAdmin):
    list_display = ['id', 'name']
    search_fields = ['name']
