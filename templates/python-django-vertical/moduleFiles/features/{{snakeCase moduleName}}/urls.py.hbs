from django.urls import path

from . import views

urlpatterns = [
    path('', views.list_create_{{moduleName}}, name='{{moduleName}}-list-create'),
    path('<int:pk>/', views.get_update_delete_{{moduleName}}, name='{{moduleName}}-detail'),
]
