{{#if ormSqlalchemy}}
from src.shared.database.sqlalchemy import db

from .{{moduleName}}_model import {{pascalCase moduleName}}


class {{pascalCase moduleName}}Repository:
    def save(self, entity: {{pascalCase moduleName}}) -> {{pascalCase moduleName}}:
        db.session.add(entity)
        db.session.flush()
        return entity

    def find_all(self) -> list[{{pascalCase moduleName}}]:
        return {{pascalCase moduleName}}.query.all()

    def find_by_id(self, pk: int) -> {{pascalCase moduleName}} | None:
        return db.session.get({{pascalCase moduleName}}, pk)

    def delete(self, entity: {{pascalCase moduleName}}) -> None:
        db.session.delete(entity)
        db.session.flush()
{{/if}}
{{#if ormNone}}
from .{{moduleName}}_model import {{pascalCase moduleName}}


class {{pascalCase moduleName}}Repository:
    def __init__(self):
        self._items: dict[int, {{pascalCase moduleName}}] = {}
        self._next_id = 1

    def save(self, entity: {{pascalCase moduleName}}) -> {{pascalCase moduleName}}:
        if entity.id == 0:
            entity.id = self._next_id
            self._next_id += 1
        self._items[entity.id] = entity
        return entity

    def find_all(self) -> list[{{pascalCase moduleName}}]:
        return list(self._items.values())

    def find_by_id(self, pk: int) -> {{pascalCase moduleName}} | None:
        return self._items.get(pk)

    def delete(self, entity: {{pascalCase moduleName}}) -> None:
        del self._items[entity.id]
{{/if}}
