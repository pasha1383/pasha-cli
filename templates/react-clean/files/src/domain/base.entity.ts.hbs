// Domain Layer — entities, value objects, repository interfaces
// Contains NO framework code, NO React imports

export interface BaseEntity {
  id: string;
  createdAt: string;
  updatedAt: string;
}
