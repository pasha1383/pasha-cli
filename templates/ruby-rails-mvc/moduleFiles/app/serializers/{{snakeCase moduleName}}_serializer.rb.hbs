class {{pascalCase moduleName}}Serializer < BaseSerializer
  def as_json(options = {})
    {
      id: resource.id,
      name: resource.name{{#if ormActiverecord}},
      created_at: resource.created_at&.iso8601,
      updated_at: resource.updated_at&.iso8601{{/if}}
    }
  end
end
