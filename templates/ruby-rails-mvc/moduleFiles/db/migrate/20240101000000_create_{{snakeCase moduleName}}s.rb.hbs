{{#if ormActiverecord}}
class Create{{pascalCase moduleName}}s < ActiveRecord::Migration[7.2]
  def change
    create_table :{{snakeCase moduleName}}s do |t|
      t.string :name, null: false, limit: 255

      t.timestamps
    end

    add_index :{{snakeCase moduleName}}s, :name
  end
end
{{/if}}
