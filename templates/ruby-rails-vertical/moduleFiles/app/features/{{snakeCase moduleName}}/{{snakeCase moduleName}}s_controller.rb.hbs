module Api
  class {{pascalCase moduleName}}sController < ApplicationController
    def index
      service = {{pascalCase moduleName}}Service.new
      result = service.list_all

      render json: {{pascalCase moduleName}}Serializer.render_collection(result)
    end

    def show
      service = {{pascalCase moduleName}}Service.new
      result = service.find(params[:id])

      render json: {{pascalCase moduleName}}Serializer.render(result)
    end

    def create
      service = {{pascalCase moduleName}}Service.new
      result = service.create({{snakeCase moduleName}}_params)

      render json: {{pascalCase moduleName}}Serializer.render(result), status: :created
    end

    def update
      service = {{pascalCase moduleName}}Service.new
      result = service.update(params[:id], {{snakeCase moduleName}}_params)

      render json: {{pascalCase moduleName}}Serializer.render(result)
    end

    def destroy
      service = {{pascalCase moduleName}}Service.new
      service.delete(params[:id])

      head :no_content
    end

    private

    def {{snakeCase moduleName}}_params
      params.require(:{{snakeCase moduleName}}).permit(:name)
    end
  end
end
