use std::sync::Arc;
use crate::errors::AppError;
use crate::{{moduleName}}::domain::repository::{{pascalCase moduleName}}Repository;
use crate::{{moduleName}}::adapters::dto::{{pascalCase moduleName}}Response;

pub async fn execute<R: {{pascalCase moduleName}}Repository>(
    repo: Arc<R>,
) -> Result<Vec<{{pascalCase moduleName}}Response>, AppError> {
    let items = repo.find_all().await.map_err(|e| AppError::internal(e.to_string()))?;
    Ok(items.into_iter().map({{pascalCase moduleName}}Response::from).collect())
}
