<script lang="ts">
  {{#each modules}}
  import { {{pascalCase this}}Widget } from '@widgets/{{this}}';
  {{/each}}
</script>

<main class="app-shell">
  <header class="app-header">
    <h1>{{projectName}}</h1>
    <p>Feature-Sliced Design (FSD)</p>
  </header>
  <div class="app-content">
    {{#each modules}}
    <{{pascalCase this}}Widget />
    {{/each}}
  </div>
</main>

<style>
  .app-shell {
    max-width: 960px;
    margin: 0 auto;
    padding: 1rem;
  }

  .app-header {
    padding: 2rem 0;
    border-bottom: 1px solid #e2e8f0;
    margin-bottom: 2rem;
  }

  .app-header h1 {
    margin: 0;
    font-size: 2rem;
  }

  .app-header p {
    color: #718096;
    margin: 0.5rem 0 0;
  }

  .app-content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
</style>
