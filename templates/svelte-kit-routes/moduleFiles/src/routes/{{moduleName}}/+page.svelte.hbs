<script lang="ts">
  import page from './+page.svelte';

  let { data } = $props();
</script>

<svelte:head>
  <title>{{pascalCase moduleName}} — {{projectName}}</title>
</svelte:head>

<h1>{{pascalCase moduleName}}</h1>
<p>Route: /{{moduleName}}</p>

<div class="content">
  <p>This is the <strong>{{pascalCase moduleName}}</strong> page. Replace this content with your feature.</p>
  <p class="counter">Count: {count}</p>
  <button onclick={increment}>Increment</button>
</div>

<style>
  .content {
    margin-top: 1rem;
  }

  .counter {
    font-size: 1.5rem;
    font-weight: bold;
    color: #2b6cb0;
  }

  button {
    padding: 0.5rem 1.5rem;
    border: 1px solid #cbd5e0;
    border-radius: 4px;
    background: #edf2f7;
    cursor: pointer;
    font-size: 1rem;
  }

  button:hover {
    background: #e2e8f0;
  }
</style>
