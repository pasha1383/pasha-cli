<script setup lang="ts">
import {{pascalCase moduleName}}Card from '@/molecules/{{pascalCase moduleName}}/{{pascalCase moduleName}}Card.vue';

defineProps<{
  items: { id: string; name: string }[];
}>();

defineEmits<{
  remove: [id: string];
}>();
</script>

<template>
  <div class="{{kebabCase moduleName}}-grid grid gap-4 md:grid-cols-2 lg:grid-cols-3">
    <{{pascalCase moduleName}}Card
      v-for="item in items"
      :key="item.id"
      :item="item"
      @remove="$emit('remove', $event)"
    />
    <div v-if="items.length === 0" class="col-span-full py-8 text-center text-gray-500">
      No items yet. Add one above.
    </div>
  </div>
</template>
